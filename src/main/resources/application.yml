spring:
  profiles:
    active: production #develop #production #staging
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1

doma:
  dialect: mysql

server:
  servlet:
    context-path: /api


jwt:
  secret: iWpJnu21sTumTsfy